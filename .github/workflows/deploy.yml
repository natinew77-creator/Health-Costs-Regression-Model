name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: risk-rate-ai/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: risk-rate-ai

      - name: Build
        run: npm run build
        working-directory: risk-rate-ai

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: risk-rate-ai/dist
          branch: gh-pages
